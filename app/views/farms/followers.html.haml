.col-md-8.col-md-offset-2
	%br
	= button_to "Go Back to Farm", farm_path(@farm), method: "get", class: "btn btn-success btn-block"
	%h1 All Users Who Follow You
	%table{:class => "table table-striped table-hover"}
		%thead
			%th Name
			%th Email
		%tbody
			- @followers.each do |f|
				%tr
					%td= f.name
					%td 
						= mail_to f.email

	%h2 Contact List
	- @followers.each do |e|
		= e.email
		= ","